# Local .terraform directories
**/.terraform/*

# .tfstate files contain a point in time snapshot of the environment and would be 
# interesting to see diffs between terraform states, audits and self-learning
# *.tfstate
# *.tfstate.*
*.tfstate.log

# Crash log files
crash.log

# Ignore any .tfvars files that are generated automatically for each Terraform run. Most
# .tfvars files are managed as part of configuration and so should be included in
# version control.
#
# example.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to add to version control using negated pattern
#
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Custom exceptions

## AWS
modules/aws-cloud-governance-workshop/*.sql

modules/aws-cloud-governance-workshop/*.sh

modules/aws-cloud-governance-workshop/*.json

modules/aws-cloud-governance-workshop/*.decrypt

## Azure
modules/azure-cloud-governance-workshop/*.sql

modules/azure-cloud-governance-workshop/*.sh

modules/azure-cloud-governance-workshop/*.json

# Apple MacBook macOS exceptions
.DS_Store